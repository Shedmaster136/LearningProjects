Steganography is a program that hides an inserted message into a BMP picture. It changes only the
bits of the lowest significance, so that the encoded message cannot be noticed by just looking at it.
